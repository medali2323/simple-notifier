name: Build Windows EXE

on:
  push:
    branches: [main, master]
  workflow_dispatch:

jobs:
  build:
    runs-on: windows-latest
    
    steps:
      - uses: actions/checkout@v4
      
      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'
      
      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install pyinstaller
          pip install -r requirements.txt
      
      - name: Build EXE avec options
        run: |
          pyinstaller --onefile --windowed --noupx --clean --name ZoneX --icon zonex.ico --add-data "zonex.ico;." --add-data "images/zonex.png;." --add-data "images/mqtt_succee.png;." --add-data ".env;." simple_notifier.py
      
      - name: Create package
        shell: pwsh
        run: |
          New-Item -ItemType Directory -Force -Path package
          Copy-Item dist\ZoneX.exe package\
          Copy-Item .env package\
          Copy-Item images\zonex.png package\
          Copy-Item images\mqtt_succee.png package\
          "Lancer ZoneX.exe" | Out-File -FilePath package\LIRE_MOI.txt -Encoding utf8
      
      - name: Create ZIP
        run: Compress-Archive -Path package\* -DestinationPath ZoneX-Windows.zip
      
      - uses: actions/upload-artifact@v4
        with:
          name: ZoneX-Windows
          path: ZoneX-Windows.zip
          retention-days: 30